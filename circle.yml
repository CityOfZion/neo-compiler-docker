machine:
  services:
    - docker

dependencies:
  override:
    - echo "n/a"

test:
  override:
    - cd neoj-builder && make run

deployment:
  feature-branch:
    branch: /^((?!master).)*$/
    commands:
      - make check-version